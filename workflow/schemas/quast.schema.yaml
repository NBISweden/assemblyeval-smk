$schema: "http://json-schema.org/draft-07/schema#"

description: quast configuration

type: object

definitions:

  quast.config:
    type: [object, 'null']
    description: Main quast tool configuration
    properties:
      ids:
        $ref: "definitions.schema.yaml#/definitions/ids"


  quast:
    description: quast resource configuration
    $ref: "definitions.schema.yaml#/definitions/resources.rule"
