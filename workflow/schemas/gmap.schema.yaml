$schema: "http://json-schema.org/draft-07/schema#"

description: gmap rules configuration

type: object

definitions:

  gmap_build:
    description: gmap build configuration
    $ref: "definitions.schema.yaml#/definitions/resources.rule"

  gmap_map:
    description: gmap map configuration
    $ref: "definitions.schema.yaml#/definitions/resources.rule"
